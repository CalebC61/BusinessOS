# Supabase — Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Used to build email confirmation redirect links (src/app/login/actions.ts).
# Set to the deployed origin in production, e.g. https://operator.example.com
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Claude API — the 5 LLM roles in AGENTS.md
ANTHROPIC_API_KEY=

# Xero — OAuth2 app credentials for the profit baseline / Sunday Review pull (M3, M7)
XERO_CLIENT_ID=
XERO_CLIENT_SECRET=
XERO_REDIRECT_URI=http://localhost:3000/api/xero/callback

# n8n — webhook base for the Sunday Review trigger (M7)
N8N_WEBHOOK_URL=
